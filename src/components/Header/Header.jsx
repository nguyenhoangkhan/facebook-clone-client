import { Search, MiddleIcon, HeaderProfile } from "./Components";

const Header = () => {
  return (
    <header>
      <Search />
      <MiddleIcon />
      <HeaderProfile />
    </header>
  );
};
export default Header;
